package Examples.TroupeLab.Server;

import core.IFCProcess;
import lattice.Authority;

public class Server extends IFCProcess{

    public Server(String id, Authority authId) {super(id, authId);}

    @Override
    protected void execute() {
        
    }
}
